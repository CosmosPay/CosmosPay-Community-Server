# Issue #36 — evidence (payment-intent state machine)

## Graph suite

```text
$ npx jest src/payment-intents/payment-intent-state-machine.spec.ts --no-coverage

Test Suites: 1 passed, 1 total
Tests:       83 passed, 83 total
```

Full adjacency matrix + terminal immutability + SUCCEEDED requires txHash.

## Invalid transition API response

`PATCH /v1/payment-intents/:id` with `EXPIRED → SUCCEEDED` returns:

```json
{
  "statusCode": 400,
  "error": "Bad Request",
  "code": "INVALID_PAYMENT_INTENT_TRANSITION",
  "message": "Invalid payment intent transition EXPIRED → SUCCEEDED: status EXPIRED is terminal and cannot be abandoned",
  "from": "EXPIRED",
  "to": "SUCCEEDED"
}
```

Covered by `test/payment-intent-transitions.e2e-spec.ts`.

## Acceptance checklist

- [x] Single transition entry point (`PaymentIntentsService.transition`)
- [x] Undeclared transitions rejected with explicit error code
- [x] SUCCEEDED requires non-empty on-chain `txHash`
- [x] Terminal statuses cannot be abandoned
- [x] Full graph tests (valid + invalid)
- [x] Consultable history via `GET /v1/payment-intents/:id/transitions`
